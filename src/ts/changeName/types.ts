type SettingsModalElements = {
	modal: HTMLDialogElement;
	input: HTMLInputElement;
	setButton: HTMLButtonElement;
	closeButton: HTMLButtonElement;
	openButton: HTMLButtonElement;
};

type NewName = {
	name: string;
};

export type {SettingsModalElements, NewName};
